export interface IProduct {
  id: number
  title: string
  code: string
  category: string
  client_type: string
  pre_sale: boolean
  featured: boolean
  image: string
  images: string[]
  description: string
}